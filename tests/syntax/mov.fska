// One line comment that should be ignored.
// Another comment that shouldn't make the compiler crash.5sas5d4654

fn custom_function {
    mov(b64 rax, b64 rbx);
    mov(b64 rcx, @b64 [rax][2][rbx] + 0x11223344);
    mov(b64 rdx, @b64 [rax] + 0x11223344);
    mov(b64 r8, @b64 [][2][rbx] + 0x11223344);
    mov(b64 r9, @b64 0x11223344);
}
